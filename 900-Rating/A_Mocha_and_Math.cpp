#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    long long tc, a, c, l, k, r, sum = 0, count = 0;
    string s, t;
    cin >> tc;

    for (long long i = 0; i < tc; i++) {
        count = 0;
        sum = 0;
        string x = "1111111111111111111111111111111111111111111111111111111111111111";

        long long n;
        cin >> n;


        vector<long long> v(n);
        for (long long j = 0; j < n; j++) {
            cin >> v[j];
        }

        

        
    }

    // long long x = 10;
    // bitset<64> b(x);
    // cout << b.to_string();  // 64-bit binary string

    for(int i=0;i<64;i++){
        cout<<1;
    }

    return 0;
}